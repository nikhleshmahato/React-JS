# React-JS
These are my React.js code files , created throughout the Learning and Development journey

Vite Bundler + ReactJS

npm create vite@latest
npm i
npm run dev

remove all unknown code file generated
understand the folder structure
display Hello World

React + ReactDOM = Web Application
React + ReactNative = Mobile Application

import & export of functions between different files
each file must have only one default export function
one file can have multiple named export functions

export default functionOne
export { fun1, fun2, fun3 }
export functionOne , { fun1, fun2, fun3 };

import functionOne from 'filename'
import  {fun1, fun2, fun3} from 'filename'
import  functionOne , {fun1, fun2, fun3} from 'filename'

JSX = JavaScript + XML tags

1. Custom Render
2. Custom Tags
3. Built in React element creation method
